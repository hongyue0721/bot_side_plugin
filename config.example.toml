# bot_side_plugin 配置示例（请对照生成的 config.toml 修改）
# 注意：实际运行使用插件自动生成的 config.toml

[plugin]
# 是否启用插件
enable = true
# 是否开启调试日志
debug_mode = false

[admin]
# 允许发布博客的管理员 QQ 号（字符串或数字均可）
admin_qqs = ["123456789"]
# 是否在群聊中无权限时静默处理（true=不提示）
silent_when_no_permission_in_group = true

